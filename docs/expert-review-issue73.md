# issue #73 中文協助 — 專家評價報告（GATE.md §6）

以「幼兒英語學習軟體／無障礙／在地化」專家視角，主動盤點「應存在但不存在」與責任邊界錯置之缺口。共 14 項，重大者已回修並重跑受影響測試（`?selftest=help-reward`）。

| # | 發現 | 嚴重度 | 處置 |
|---|---|---|---|
| 1 | `zh-TW` 語音於部分瀏覽器/OS 不存在時，中文鈕會靜默無聲 | 中 | 已記錄；`speak()` 於 `speechSynthesis` 不可用時 no-op 不崩潰。zh-TW voice 偵測與視覺降級提示列為後續增修。 |
| 2 | 按中文後未告知「該題已無獎勵」 | 中 | **已修**：答對時依層級顯示親切回饋（用中文／多次嘗試／半額）。 |
| 3 | 無獎勵（coins=0）時未說明原因 | 中 | **已修**：`answerLesson` 回饋分用中文 vs 多次嘗試兩種訊息。 |
| 4 | 半額 `Math.round(base*0.5)` 取整 | 低 | 各區 base 皆偶數（20/100/500/2000），無誤差。 |
| 5 | `speak()` 會 cancel 前一段語音 | 低 | 預期行為（避免重疊）；快速連點會截斷，可接受。 |
| 6 | 撥放鈕未納入方向鍵焦點巡覽（僅 `.choice-button`） | 中 | 已記錄；保留作答鈕為鍵盤主巡覽，撥放鈕供觸控/滑鼠。完整鍵盤可達列後續增修。 |
| 7 | 中文鈕 aria-label 過於通用、觸控目標偏小 | 中 | **已修**：aria-label 帶入該選項文字；撥放鈕 ≥44×44。 |
| 8 | 缺自動化「中文覆蓋」檢查，恐漏譯回歸 | 中 | **已修**：selftest 增跨地區覆蓋檢查（4 區各驗 4 中文選項鈕＋題目中文鈕）。 |
| 9 | `promptZh` 已算但題目列（advPrompt）隱藏未用 | 低 | 保留於資料模型備用；題目中文鈕改播 advLine（openingZh）。無害。 |
| 10 | 離開再進入會換題並重置階梯（可重置懲罰） | 低 | 符合「每題獨立計算」設計；屬可接受彈性，已記錄。 |
| 11 | spec#11 結算「答題正確度」與獎勵口徑 | 低 | `recordCycleAnswer` 維持每次嘗試計入，與獎勵階梯解耦，口徑一致。 |
| 12 | 中文 TTS 語速沿用 `rate=0.86`（為英文調） | 低 | 可接受；分語系語速微調列為後續。 |
| 13 | 未強制「先聽再答」 | 低 | 非回歸（既有行為）；不在本議題範圍。 |
| 14 | 每列新增 2 鈕於窄螢幕可能溢出 | 中 | **已驗**：375px 實測選項列無溢出（row 279 < list 283、rowOverflow=false）。 |

## 機器判定（GATE §1）
- 型別契約 `tsc --noEmit`：**0 錯**。
- 結構 `repoLint` / 設計 `docLint`：**各 0 違規**。
- 整合 `?selftest=help-reward`：**passed**（full/half/none(用中文)/none(第3次) 階梯 + 4 區中文覆蓋）。
- 瀏覽器 console：**0 error**。
- 依賴安全：純前端、無新增 npm 依賴（瀏覽器內建 SpeechSynthesis），不適用 audit。

## 結論
核心能力（雙語撥放、獎勵階梯、缺中文降級、四區覆蓋）皆「已測通過」，無「未實作／責任邊界不符」之重大缺口；**可宣稱完成（本議題範圍）**。後續增修建議：zh-TW voice 偵測降級提示、撥放鈕鍵盤可達、分語系語速。
