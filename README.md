# solKidGalGame — Princess English Adventure

> 本 README 是本專案的**產品手冊（productReadme）**，從玩家與家長／維護者「實際怎麼用」的角度撰寫。
> 內部設計與架構的單一事實來源改為 [docs/design.md](docs/design.md)（formatVersion 2.0）；本檔隨已完成的 dev 項目逐步校準。

陪 Princess Lumi 到不同地點和角色用一句短英文對話，答對拿 coins、日記與學到的單字，再把 coins 換成看得見的髮型、衣服、鞋帽與配件。**學英文與換裝獎勵是同一個正向循環。**

## 這是什麼

- 一個給**年幼英文學習者**玩的靜態網頁、日式 ADV 風格英文練習遊戲。
- 主要在**手機瀏覽器直向**遊玩，桌機也可用。
- 以 **GitHub Pages 純靜態**部署，無需後端、無 build step。

不做的事：不做 landing page、不做大型課程平台、不做密集 phonics 課程、不做後台商品管理、不做網路登入／密碼／雲端同步（多人帳號為同一瀏覽器內的本機帳號，選角僅切換本機外觀）。

## 怎麼玩

核心每一輪只需理解一件事：**選地方 → 聽一句 → 選一句英文 → 拿獎勵 → 幫公主變可愛**。

```text
選擇帳號（每次進入先選，可新增／刪除帳號）
  → 選角＋命名＋設定視覺主題：新帳號隨機識別色＋背景花紋，可改粉彩色盤／調色器自訂／改花紋
  → Princess Room → Castle Map → World Map → Area Map
  → 進入地點 Scene → 選動作 → 生活聊天 / 打工 / 逛店 / 換裝
  → 回饋（心情 / coins / 日記 / 學到的字 / 換裝）→ 再來一輪
```

- **選擇帳號**：每次進入遊戲會先到帳號選擇畫面，選擇要用哪個帳號遊玩；可新增帳號（建立全新進度）或刪除帳號。帳號卡會顯示該帳號公主的頭胸部大頭照（以識別色半透明底色鋪底）、最近遊玩時間、coins 與目前可玩／休息狀態，方便多人共用同一裝置時快速分辨。不同帳號的 coins、日記、學到的字、擁有與穿搭、所在位置都各自獨立、互不混用。帳號只存在這台裝置的這個瀏覽器，不需要也不會做網路登入或雲端同步。
- **選角與命名**：首次進入會先選公主外觀、輸入名字並設定視覺主題——新帳號會先取得一次性隨機識別色與背景花紋（如波浪、泡泡、格紋等），玩家可改從飽和度較低的粉彩色盤選擇、用調色器自訂任一色，或改選背景花紋；之後可再叫出此畫面重選外觀、改名、改色或改花紋（不影響既有進度）。可玩公主 roster 為 Lumi、Yumi、Rosa 三位；既有帶 `sol`（舊名 Mary）角色 id 的舊存檔於讀取時自動 fallback 為預設角色 `lumi`，可無縫升級至三角色 roster。選單、帳號卡與遊戲內人物資訊欄的大頭照卡片皆以該識別色的半透明底色鋪底。
- **地圖導覽**：World Map、Castle Map 與各地區地圖都有可移動的公主（鍵盤方向鍵走動），公主圖示放大後更醒目、清楚可見（不再使用識別色背板）。在 World Map 點選地區（Castle / Urban / Rural / Wild）時，公主會先走到該地點再進入，移動途中再點一次即略過直接進入；進入地區後於地區地圖再選地點。地區間移動一律先回 World Map 再進入。各地點的位置貼合地圖背景（例如出口在城門或道路、釣魚在河邊、商店在城內），不會擠成一團。進入地點後的 ADV 場景背景應是完整 `1024x1024` 童話手繪圖；若上下區域看起來只是模糊或延展補版，應回報為場景美術資產問題，而不是操作或瀏覽器問題。
- **答英文題**：在有 lesson 的地點聽情境句、從選項選出正確英文，答對得獎勵與學習紀錄；題目會貼合該場景主體、依地區英文等級分級（句型由淺到深），且由場景角色以第一人稱直接對公主說話、是貼近生活的對話（不是第三人稱旁白或考試題幹）。題目與每個選項都可分別**撥放英文**或**撥放中文**，但用中文會影響獎勵——見下節〈中文協助與獎勵階梯〉。答對獲得的 coins 會**即時顯示在對話場景畫面上**，不用離開場景就能看到金錢增加。
- **場景互動三模組（生活聊天／逛店／打工）**：生活聊天在每個可互動場景（含商店）預設都能進行，逛店與打工依場景開啟——詳見下節〈場景互動：生活聊天、逛店、打工〉。
- **商店與換裝**：用 coins 在各地商店（逛店模組）購買外觀，於衣櫃試穿與穿戴；衣服、鞋帽與配件會依類別級對位範圍穿到公主身上，不需要的可退款換回 coins。**公主自己的房間用單一「換裝」按鈕打開右側衣櫃，和商店是同一套多欄貨架面板**——但衣櫃只穿、不買賣：每件衣物的**深粉紅按鈕**按一下穿上、字會變成「脫下」，再按一下就脫下；「換裝」入口按鈕本身與其他場景的按鈕一樣、不特別上色。在**桌機寬螢幕**，衣櫃（換裝與商店）面板會加寬、**一次完整列出所有可選衣物**不需捲動，且面板位於**公主立繪之後**，換裝時公主維持完整可見、不被面板遮擋。**新建帳號的公主以一套得體的入門造型起步**，且一開始只擁有身上所穿的那幾件，其餘髮型、衣服、鞋帽與配件都要靠答題賺 coins 到各地商店購買——讓「學英文→換裝」的成長從第一次遊玩就開始。
- **衣物對位調整（需本機 server）**：衣櫃中每件已擁有的衣物右側另有「**調整**」按鈕，點擊後以全螢幕 overlay 在不離開遊戲的前提下即時調整該單品的**位移、縮放與旋轉**——以五組滑桿（中心 X、中心 Y、寬、高、旋轉 -180°～180°）即時更新 overlay 內的 paper-doll 預覽；確認後按「儲存」將新對位寫回 sidecar，overlay 關閉後遊戲立即套用不需整頁重整；按「取消」丟棄本次調整，遊戲回到原位。此功能需 `node server.mjs` 在本機或家庭 LAN 中執行（公開 GitHub Pages 可預覽但儲存會失敗並顯示提示）。本功能不提供四角任意變形（warp/corners），對位調整限矩形邊界（left/top/right/bottom）配合旋轉。（2plan 初稿，待 dev／opr 校準。）
- **角色輪廓與陰影**：ADV 場景人物、公主紙娃娃、地圖上的公主圖示與頭胸部大頭照，應以透明角色外框產生清楚描邊與自然陰影，讓人物在複雜背景上仍可辨識。試穿時的亮色光暈只代表「正在試穿」狀態，不是角色常態可讀性的主要來源。
- **切換帳號**：進入遊戲後可用返回初始選單按鈕回到帳號／公主選單，切換玩家或調整公主設定；返回不會重置進度，也不會解除尚未結束的休息鎖定。
- **操作**：支援觸控、滑鼠、方向鍵 / W·S、Enter、Space、數字鍵；場景互動採兩層動線——第一層場景選單用 `Leave` 回地圖，進入任一互動（生活聊天／打工／逛店／退款／換裝／提示）後一律用 `Back` 回到場景選單，可接著選別的互動。

## 場景互動：生活聊天、逛店、打工

> 本節為 2plan 初稿，行為細節待 dev／opr 校準。

每個地點用同一套場景模板，以模組旗標統一宣告要開啟的互動（不以商店為特例）：**生活聊天在每個可互動場景（含商店）預設都能進行**（公主自己的房間與城門除外），逛店與打工則依場景選擇性開啟。

- **生活聊天（Chat）**：場景人物會以第一人稱主動向公主寒暄（例如「早安，公主，今天海面很平靜」），公主再從選項中選出合適、口語自然的回應（像真的在聊天、可帶語氣詞，而非教科書例句），**每個可互動場景（含商店）都能聊**。聊天是輕鬆互動，每題只給 **2 個選項**。答對會**提升心情**並在護眼上限內**延長這次的可玩時間**——用意是讓孩子感受到「和人互動是滿足自己的需求（心情變好、想多待一會兒）」，而不是為了換取別人的回饋。聊天不發 coins。
- **逛店（Shop）**：用 coins 購買外觀並換裝（沿用既有商店機制）。
- **打工（Part-Time Job）**：場景人物會以第一人稱向公主提出一個**需要動腦的請求或抉擇**（例如「白花一直賣不完，我該怎麼辦？」、該先做哪件事、東西怎麼安排），公主從 **3 個選項**中選出**經過思考的決策、判斷或建議**來幫忙、賺取 coins——回應是真的在替對方拿主意、做選擇，而不是把對方的指令或眼前正在做的動作覆述一遍。公主答應幫忙時可以先說一句自然的應允語（「好啊，我可以…／沒問題／我來想想」）再接實質的判斷。任務可結合簡易數學或生活常識（例如算庫存、排順序）。打工所得即為勞動報酬。

一句話分流：**聊天回饋心情（精神需求）、打工回饋金幣（勞動所得）**，逛店則是花錢。

在同一個場景裡，做完一種互動會**回到場景選單**（`Back`），可以接著選別的——例如**聊天完再逛店**，不會被迫直接跳出場景；要離開場景回地圖時，在場景選單按 `Leave`。

## 快速開始

線上：開啟你部署的 GitHub Pages 網址即可遊玩。

本機：

```powershell
python -m http.server 4173
```

也可使用本 repo 的本機靜態伺服器：

```powershell
node server.mjs
```

本機網址依所用指令：`python -m http.server 4173` → `http://127.0.0.1:4173/`；`node server.mjs` →（預設）`http://127.0.0.1:4174/`。

## 存檔與還原

- 進度（coins、穿搭、日記、位置、所選角色、名字與識別色）會以**目前帳號**為單位自動存入瀏覽器本機儲存，重整後還原；切換帳號會載入該帳號自己的進度。
- 可於 Save / Load 介面**匯出 Markdown 存檔**並再次**匯入還原**（以目前帳號為範圍）；缺漏或舊格式存檔會自動正規化回安全預設。

## 可玩公主與換裝分層

> 本節已於 dev（issue #123）實作；待 opr 終驗。

可玩公主整理為三位可辨識角色：

- **Princess Lumi**：保留既有明亮公主方向。
- **Princess Yumi**：重製為深藍髮、冷色優雅系外觀，降低與其他角色的同質性。
- **Princess Rosa**：棕髮甜美系公主，但不把禮服、皇冠或背景固定進角色底圖。

（原 **Princess Mary**／舊 `sol` 已於 issue #277／#285 自 roster 移除；既有帶 `sol` id 之舊存檔讀取時自動 fallback 為 `lumi`。）

紙娃娃立繪採**共用 `body`（neck-down、含永久肌膚安全底著／粉色居家底層，使角色永不裸露）＋ 每位公主一張 `head`（臉＋預設髮型，承載髮色與識別）** 之分層合成：三位角色共用同一張 `body`、各自一張 `head`，皆為 `shared-512x768-v1`、`512x768` 透明 WebP，與同一套 wardrobe layer 對位。換裝時衣物疊在 `body` 底著之上、髮型 layer 須完全覆蓋 `head` 的預設髮，因此更換衣物或髮型時舊的不會殘留（解決過去 baked-in 底圖換裝後舊髮舊衣仍透出的雙重疊圖）。`body`／`head`、wardrobe layer 與商品縮圖必須使用 GPT 產生或手工修圖的童話手繪風格 PNG／WebP，不使用 SVG、CSS 濾鏡或 renderer 特例代替正式素材；issue #163 的 Yumi 深藍髮與 Yumi 眼睛之 Rosa 眼睛校準改由各 `head` 素材承載，底著、姿勢、比例、透明底、頸部接縫與對位不得改動。衣物、鞋帽與配件的 layer 對位採類別級上下左右邊界／安全框，同類衣物統一遵守同一對位範圍，新增商品不應每件另做一次性 nudge。新增公主只需新增一張 `head`、共用同一 `body`。選角畫面與頭胸部大頭照由 `body`＋`head` 合成後等比裁切頭胸部呈現（不再裁切單張 baked-in `base.webp`、不另維護獨立 `thumb.webp`）。內部仍保留 `softBrownHair`／`starterPajama` 等 starter item id 以相容舊存檔，正規化後得到正確的預設外觀、不重複疊圖。

衣櫃的服裝分類精簡為四類——髮型、整件服裝（outfit）、鞋子與配件（帽子併入配件）：不再分開上衣與下身（top／bottom），整件衣著統一以 outfit 呈現（原洋裝 dress 改名 outfit）；既有舊存檔載入時自動相容——清除已移除的上衣／下身欄位、把舊的 dress 欄位轉為 outfit。（issue #251，2plan 初稿，待 dev／opr 校準。）

三位公主的大頭照與帳號識別共用同一套頭胸裁切設定：遊戲內人物資訊欄與帳號卡的大頭照會反映該帳號目前的即時穿搭（髮型與衣著裁切為頭胸）；選角畫面與公主選單因選角當下尚未套用衣櫥而呈現各公主基本造型；皆不使用另一套縮圖。Lumi、Yumi、Rosa 的新帳號初始識別色與背景花紋由合法集合一次性隨機取得並保存；玩家命名或日後調整公主時可從粉彩色票、調色器與背景花紋選項自行調整。這個識別色會用於大頭照背景與帳號辨識；地圖上的公主圖示不再套用識別色背板。

角色的常態圖地分離以透明輪廓描邊與自然景深陰影完成：ADV NPC 立繪可有較明顯的深色輪廓與後方陰影，地圖 token 與頭胸照則要保持銳利、不要遮擋 UI。試穿狀態可額外出現亮色光暈，但關閉試穿後不應殘留；新增衣物或角色時，需檢查多層 wardrobe layer 不會因陰影逐層疊加而變成髒邊。

## 遊玩時間與休息（護眼）

> 本項為 2plan 初稿，行為細節待 dev／opr 校準。

為保護兒童視力，連續遊玩會逐步用掉「遊玩時間」：

- 開始遊玩後，遊玩時間會依真實時間逐步減少；用完時會自動結算這一回合的成果（這回合獲得的金錢、答題數與答題正確度）。
- 人物資訊欄會顯示本次可玩時間額度（基礎時長；生活聊天延長時會把多出來的分鐘一起標示出來）與剩餘可玩時間，不只顯示百分比。
- 結算後進入**強制休息**，休息時間結束前無法繼續遊玩，時間到才解鎖；休息／結算畫面可返回初始帳號／公主選單，但選回同一帳號仍維持休息鎖定。
- 每次**遊玩時長**與**休息時長**預設各 15 分鐘，可在設定調整。
- 在護眼上限內，**生活聊天答對可延長這次的可玩時間**（最多到護眼上限為止，不會無限延長）。
- 遊玩時間與休息以**各帳號各自計算**，切換帳號各自獨立計時。

## 中文協助與獎勵階梯

> 本節已於 dev（issue #73）實作；以下行為以實作為準，待 opr 終驗。

看不懂英文時可以先用中文理解題意再作答；但為了鼓勵先試英文，用不用中文會影響獎勵：

- 答題畫面的**題目**與**每個選項**都有兩個語音鈕：**撥放英文（En）**與**撥放中文（中）**。
- 獎勵階梯（每題各自計算）：
  - 沒按中文、**第一次**就答對 → **全額** coins。
  - 沒按中文、**第二次**才答對 → **半額** coins。
  - **按過中文**，或**第三次（含）以後**才答對 → **沒有** coins。
- 撥放英文不影響獎勵，只有按中文才算使用協助；換到下一題時重新計算。
- 中英文語音使用瀏覽器內建 Web Speech API；不同裝置可用的中文／英文 voice 不一，遊戲會優先選擇相符語言的 voice，缺少時退回可用預設聲音並繼續遊戲。

## 角色聲音

> 本節穩定播放與診斷已於 dev（issue #109）實作；依角色類型指定語音與首字前置留白已於 dev（issue #134）實作並以 `?selftest=voice` 驗證，實機聽感仍待 opr 終驗。issue #246 規劃把「角色語音指定」由玩家設定移入維護者用的管理設定工具（聲音管理頁籤），公開遊玩端改為一律自動選用——本段對應描述為 2plan 初稿，待 dev／opr 校準。

為了讓角色更有臨場感，不同人物會用**貼合自己角色的聲音**說話，而不是全部同一個語調：

- 場景裡的人物（國王、王后、廚娘、騎士⋯）會依各自特性（性別、年齡、性格等）以不同聲音念出對白與開場白。
- 你操作的**公主**在**選定一個選項作答**時，會用公主的聲音把你選的答案唸出來。
- 這些語音沿用畫面上既有的 **Voice 開關**：關閉時所有角色語音都靜音。
- 聲音以瀏覽器內建 Web Speech API 呈現；系統會等待瀏覽器 voice 清單載入後挑選合適 voice，缺對應設定時自動退回預設嗓音，不會出錯或卡住。
- 公開遊玩端的角色語音**一律由系統自動選用**：依各類角色（性別×性格）挑選同性別的合適瀏覽器語音，沒有指定的類型會自動沿用同性別的指定，再不然依語言挑選合適 voice；角色的年齡等差異仍以音高／語速表現。一般玩家／家長不需要也無法在遊戲設定裡逐一挑選語音。
- **角色語音指定改為維護者作業**（issue #246）：要把某類角色（依**性別與性格**，例如男性成年／少年、女性成年／少年等類型）綁定到特定的瀏覽器語音（如 Windows 內建語音），由維護者在本機 dev 環境開啟**管理設定工具**的**聲音管理頁籤**設定；此設定為整台裝置共用（device-wide），公開的 GitHub Pages 網站不提供這個入口。
- 為了讓兒童更容易聽清楚，語音會以**較慢的速度**朗讀（基準約為原本速度的 90%），英文與中文協助都一樣放慢；各角色之間的相對快慢仍會保留。
- 為避免中英文與角色語音第一個字被吃掉，語音在送出前會於**開頭加入少量空白**延後首字，並以集中式 speech manager 管理佇列、重播與停止，不會在每次播放前無條件取消既有語音。
- **離開場景時**（關閉對話、切換場景或返回地圖）會即時停止正在播放的語音，避免聲音殘留到下一個畫面；受瀏覽器限制無法讓語音逐漸變小聲淡出時，改以即時停止處理。
- **在場景裡進出子互動（聊天、逛店、打工等）或返回場景選單時**，也會即時停止前一段語音、改接當下的話題，不讓上一段聲音蓋過現在的畫面。
- **同一個場景的歡迎詞每次造訪只播一次**：第一次進入場景時角色會打招呼，之後在子互動與場景選單之間往返不會重複播放；離開場景回地圖後再次進入，才會再招呼一次。
- 維護者可用語音診斷紀錄檢查實際採用的 voice、語言、pitch、rate、播放事件、錯誤代碼與 fallback 原因；這些紀錄用來判斷工程流程與瀏覽器限制，最終仍需實機聽測確認兒童聽感。

## 關於與版本沿革（About）

> 本項為 2plan 初稿，版本沿革回填內容待 dev／opr 校準。

設定選單新增 **About** 頁籤，集中呈現「關於這個作品」的資訊：

- **開啟方式**：點畫面右上角 ⚙ 開啟設定選單，切到 **About** 頁籤。
- **版權宣告**：`carlton0521@gmail.com, copyright reserved, 2026`。
- **版本沿革**：以中文短主旨列出歷次版本「改了什麼」，方便家長與孩子了解每次更新（預設呈現最近 10 個版本）。
- 原本顯示在 Settings 的版本與建置時間，改併入 About 一併呈現，Settings 不再另列版本卡。

## 部署（GitHub Pages）

- 使用 `Deploy from a branch`，靜態網站 root 選 **repository root**，入口為 `index.html`。
- repository root 必須保留 `.nojekyll`（否則 `_shared` 等底線開頭資料夾會被 Jekyll 略過，造成 ES module 404）。
- `server.mjs` 只用於本機靜態預覽，不是 GitHub Pages 必需項；預設監聽 `0.0.0.0`，啟動 log 會顯示 LAN IP，同一家庭區網內的手機或平板可直接以該 IP 開啟管理設定工具（`tool/wardrobe-tuner.html`）。若需指定監聽位址，可設 `HOST` 環境變數覆寫（例：`HOST=127.0.0.1 node server.mjs`）。

## 擴充內容（給維護者）


本機 dev 環境提供**管理設定工具**（`tool/wardrobe-tuner.html`，公開 GitHub Pages 不提供入口），維護者用它調整各項組態。其導覽**依 `content-package/` 的內容資料包組織**：頂層為公主、衣物、地圖與場景、聲音、遊戲規則等資料包，地圖與場景再依「世界 → 地區 → 地點／場景 → 對話」分層；各設定（衣物單品與投影對位、**衣物旋轉角度**、地圖座標、場景對話、角色語音指定、新局起始等）歸入其所屬資料包節點，新增內容或設定頁時沿資料包結構擴充、不必重排既有動線。**衣物旋轉**可在工具頁找到目標衣物後拖動「旋轉（°）」滑桿（-180°～180°）即時預覽，確認後點「儲存」寫回 sidecar；遊戲重整後即套用新旋轉。工具 UI 採 `[hmiIntf通用視覺規範]`（MD3 基座之管理網站規範），主題色沿用遊戲低飽和粉彩識別色系（種子見 `docs/design-visual/`）。（本段為 2plan 初稿，導覽與視覺收斂待 dev 落地校準。）


area、角色、可玩公主、衣物與場景背景都是 `content-package/` 下的**模組化內容包**，新增／調整內容時優先只動單一包與少量 registry 設定，不必改核心引擎。可玩公主須共用 `shared-512x768-v1` rig，立繪採共用 `body`（含永久肌膚安全底著）＋ per-character `head`（含預設髮型、承載髮色與識別）分層合成，衣物與髮型 layer 疊於其上、更換時舊層不殘留（髮型 layer 須完全覆蓋 `head` 預設髮）；新增公主只需新增一張 `head`、共用同一 `body`。新增或重製 wardrobe 素材時，正式 layer 與縮圖須以 GPT 產生／修圖為童話手繪風格 bitmap，轉透明 WebP／PNG，禁止使用 SVG 作為服裝素材或完成品替代；同類衣物須沿用類別級 layer bounds，並以實際穿上後的手機直向與桌機視覺 QA 判斷對位。新增或替換 ADV 場景背景時，正式資產須是完整繪製的 `1024x1024` WebP，不可用上下模糊、延展、frosted cover、CSS 濾鏡或 renderer 特例補足尺寸；合理景深可以保留，但不得替代原本應繪製的場景內容。（注意區分：桌機／寬視口下，固定比例內容「內容區外」的 letterbox 留白會由跨檢視共用的 stage 機制以該畫面背景的模糊放大版鋪底，這是內容區外的沉浸鋪底、與上述「場景內容本身不可用模糊補版」是兩件事。）新增角色、衣物或場景後，也要以手機直向與桌機視口檢查角色透明輪廓描邊、自然陰影與試穿光暈是否分離，不能用大範圍糊化發光替代角色本體輪廓。各類圖像資產都有**標準尺寸與檔重預算**（角色 body／head／NPC 512×768、ADV 場景 1024×1024、地區地圖 1536×1536、世界地圖 1024×1536、衣物縮圖 256×256、衣物 layer 512×768），新增或替換時除像素尺寸要正確外，**檔案大小也不可超出該類別預算**——過大的圖檔會拖慢純靜態載入，須先重壓縮到預算內（維持尺寸與童話手繪觀感）或登記具名豁免才能納入；`?selftest=data-audit` 會一併檢查全資產的尺寸與檔重並列出超標清單。資料夾慣例、manifest 章節、角色 `512×768` rig 與美術尺度等細部規則，見：


- 內部設計 SSOT：[docs/design.md](docs/design.md)
- 角色尺度與美術契約：[contract-local/hmiIntf自訂角色尺度與美術規範.md](contract-local/hmiIntf自訂角色尺度與美術規範.md)
- 技術選型與契約：[contract-common/](contract-common/)、[contract-local/contract-index.md](contract-local/contract-index.md)
- agent 操作規則：[AGENTS.md](AGENTS.md)

## 成功判定

- 兒童能在短回合、低挫折下完成英文練習並獲得即時回饋。
- 看不懂英文時能用中文理解題意，且獎勵階梯鼓勵先試英文（越早不靠中文答對獎勵越高、用過中文則該題無獎勵）。
- 「答題 → 獲獎勵 → 換裝」可在單次遊玩內成環，外觀有看得見的改變。
- 三位可玩公主可被清楚辨識；Yumi 為深藍髮（舊帶 `sol`／Mary id 之存檔讀取時 fallback 為 `lumi`）；立繪由共用 `body`＋per-character `head` 合成、頸部接縫對位、無黑底、尺寸對位正確，更換衣物或髮型後舊層不殘留（髮型完全覆蓋 `head` 預設髮）、舊存檔 starter 相容項不重複疊圖；角色 `body`／`head`、wardrobe layer 與商品縮圖使用 GPT 產生或手工修圖的童話手繪風格 raster 素材，不用 SVG 或濾鏡代替；代表性衣物實際穿上後位置、比例、接縫與跨角色共用 layer 對位正確；頭胸部大頭照、識別色（新帳號隨機取得、粉彩或自訂，以半透明底色鋪在卡片）與背景花紋在選單、帳號卡與遊戲內人物資訊欄一致（資訊欄與帳號卡反映目前穿搭、選角與選單呈現基本造型）；地圖公主圖示放大且不再套用識別色背板。
- ADV NPC、公主紙娃娃、地圖 token 與頭胸照在手機直向與桌機視口皆有清楚角色外框；常態效果是貼合透明輪廓的描邊與自然陰影，不是大範圍糊化光暈；試穿光暈只在試穿狀態出現，關閉後不殘留。
- 不同場景人物與玩家公主各有貼合角色的聲音，公主會唸出所選的答案；公開遊玩端的角色語音一律自動選用（依性別×性格挑同性別合適語音），角色語音指定改由維護者於管理設定工具的聲音管理頁籤設定（issue #246，dev-only）；中英文與角色語音首字清楚（開頭前置留白）、語速約 90%，缺合適瀏覽器 voice 時會降級並記錄；關閉 Voice 後語音靜音、遊戲仍可玩。
- 進度可保存並還原；首次選角、命名、識別色（粉彩／調色器自訂）與背景花紋設定順暢，遊戲內稱呼隨名字更新（品牌名 Luminara 不變）。
- 多人共用同一裝置時，可用各自帳號保留獨立進度；新增、選擇、返回初始選單切換與刪除帳號順暢，刪除使用中或最後一個帳號後回到帳號選擇而不殘留錯誤進度。
- 連續遊玩達設定時長會結算本回合成果並進入休息，休息結束前無法續玩；遊玩與休息時長可於設定調整、各帳號各自計算，返回初始選單不會繞過同帳號休息鎖定。
- 場景互動分為生活聊天、逛店與打工三種，以模組旗標統一宣告、不以商店為特例：生活聊天在每個可互動場景（含商店）都能進行、每題 2 選項；打工每題 3 選項；聊天與打工的對白皆由場景角色以第一人稱對公主發話、選項為公主的回應，且公主回應力求口語自然（打工答應幫忙時先以應允語句開頭再接回答）。聊天答對提升心情並在護眼上限內延長可玩時間，打工答對賺取 coins，回饋型別清楚分流。
- 桌機或寬螢幕遊玩時，地圖與場景等固定比例畫面置中後左右（或上下）露出的留白，會以該畫面背景的模糊放大版鋪底，看起來像沉浸的背景延伸而非空白邊；這層模糊只在內容區外，畫面內容本身仍維持完整清楚、不被模糊，手機直向因畫面填滿寬度通常看不到這層鋪底。
- 維護者能以純靜態方式部署，並可模組化擴充內容而不影響既有功能；場景背景維持完整繪製的 `1024x1024` WebP，沒有以上下模糊、延展或 renderer 特例補版撐滿尺寸。
- 維護者可在本機 dev 環境用管理設定工具，依內容資料包結構（公主／衣物／地圖與場景／聲音／遊戲規則；地圖與場景再依世界→地區→地點/場景→對話）集中找到並維護各項組態，新增設定頁時沿資料包結構擴充而不打散既有動線；此入口公開 GitHub Pages 不提供。

## 參考案例

| 參考 | 借用方向 |
|---|---|
| [Khan Academy Kids](https://en.khanacademy.org/kids) | 角色陪伴、短任務、學習足跡 |
| [Duolingo ABC](https://abc.duolingo.com/) | 短回合、低挫折、即時回饋 |
| [Lingokids](https://help.lingokids.com/hc/en-us/articles/23532720590610-Playlearning-Sections) | Playlearning 與兒童可自行操作的導覽 |
| [Toca Boca World](https://www.tocaboca.com/app/world/) | Dress-up、自我表達、角色扮演 |

## 變更紀錄

- 2026-07-03（issue #298）：內部結構重構，**不影響玩法、玩家無感**。把累積成巨石的核心程式 `game-engine/main.js`（4052 行、282 個函式擠在一檔）依關注點拆解為模組——語音、ADV 場景流程、商店／衣櫃、地圖、HUD 渲染、遊玩時鐘、持久化、選單畫面、事件接線各自歸位（沿既有 `game-engine/` 資料夾慣例、不另創架構），`main.js` 收斂為 256 行的組裝與調度；同時把層層補丁疊加的 `styles/mobile.css`（4083 行）內同選擇器重複覆寫全數收斂歸零、依畫面域解體為八個 ≤800 行的分層樣式檔。全程行為零變更：以 40 張手機直向＋桌機雙視口截圖做像素級回歸比對、全部 21 個 selftest 與 tsc 維持綠。並新增**結構守門** `node scripts/structureLint.mjs`（單檔行數上限、CSS 重複規則塊歸零，納入常備守門），防止巨石長回。順帶修復三處既有守門缺陷（兩件 urban hand 配件長邊貼滿重正規化＋等視覺補償對位、monkey 測試誤點 dev 導航鈕、voice 測試套件順序相依）。設計決策見 [docs/design.md](docs/design.md)（模組實作對照、paramStructureQualityBar、intTest#66）。

- 2026-06-25（issue #259）：內部整理，**不影響玩法、玩家無感**。把遊戲「起始狀態」的設定（過去全部擠在同一個 `default-state.js` 物件裡）依語意在**同一個檔案內**整理成三個具名區塊——**公主新局設定**（角色／名字／識別色／花紋／coins／已擁有衣物／穿搭與起始進度）、**起始位置**（所在地區／地點／座標）、**遊戲規則與家長**（遊玩／休息時長等護眼設定、難度、語音開關等）；對外仍合成同一個 `defaultState`，與 #260 管理設定工具的三個分頁（公主／地圖／遊戲規則）關注點一致，讓日後維護更清楚。**起始值與舊存檔相容完全不變**，只是內部分組更整齊（不拆多檔、不另建相容層）。`mood` 既有的小矛盾另案處理（#262）。設計決策詳見 [docs/design-issue259.md](docs/design-issue259.md)。本項為 2plan 初稿，待 dev／opr 校準。
- 2026-06-22（issue #246）：設計把「角色語音指定」從玩家設定搬到維護者用的**管理設定工具**。過去玩家／家長可在遊戲內 **Settings → Character voices** 為各類角色（性別×性格）逐桶挑選瀏覽器語音；但這項其實是整台裝置共用、需要反覆試聽比對才挑得定的**設定工作**，比較像維護者的事，放在玩家設定裡反而與「Voice 開關」「遊玩時間」等玩家控制混在一起。改為在管理設定工具（`tool/wardrobe-tuner.html`，僅在本機 dev 環境出現、公開 GitHub Pages 不顯示）新增一個**聲音管理頁籤**，把角色語音指定整段移過去並沿用同一套語音清單／推薦分組／持久化機制（單一事實來源、不另寫第二套）；玩家設定只保留 **Voice On/Off** 總開關。公開遊玩端因此不再提供語音指定入口，未指定的角色類型一律由系統自動依性別與語言挑選合適語音（既有 Auto 行為）。連帶把設計文件 spec#2／solCase#12.5 的語音指定位置由「玩家於設定」回修為「維護者於管理設定工具」（已經 USR 於 obj 階段核准方向）。設計決策詳見 [docs/design.md](docs/design.md)（spec#2、solCase#12.5、sysCase#9.5、intTest#41、docProgTest#12／e2eTest#11）。本項為 2plan 初稿，待 dev／opr 校準。
- 2026-06-22（issue #252）：修正城堡地圖入口（城門）圖示在公主接近時異常消失。過去公主走近城堡入口時，入口的 🏰 圖示會整顆不見，看起來像 bug。原因是地圖標記的「超出畫面就隱藏」是一套統一機制，會要求**整個圖示外框**都落在地圖可視範圍內才顯示；而城堡入口的位置貼著地圖最底緣，當公主靠近、入口圖示被放大highlight時，放大後的外框下緣會戳出畫面邊界幾個 pixel，於是整顆入口就被當成「超出畫面」藏起來（並非有什麼針對城門的舊程式碼）。改為**以圖示的中心點是否在可視範圍內來判定**——圖示中心在畫面內就代表這個地點可以走到、應該顯示，只有中心真的被移出畫面才隱藏。此調整是單一共用機制、世界／城堡／地區三地圖一致生效，沒有針對城門的特例，也不動地點座標；且只會讓更多貼邊圖示顯示、不會讓任何圖示消失（不退化）。已新增 `?selftest=marker-visibility` 守門（貼邊圖示中心在內須顯示、中心移出畫面須隱藏），並回歸 `map-avatar`／`map-walk`／`scene-nav`／`data-audit`／`monkey`，0 console error。設計決策詳見 [docs/design-issue252.md](docs/design-issue252.md)。本項為 dev 實作，待 opr 終驗。
- 2026-06-21（issue #210）：設計把衣物改成「一個地區一家服飾店、一家店賣一個資源包」。過去同一個地區會有好幾家衣物相關的店、每家只賣一兩種類別（例如市區就有洋裝店、髮廊、裁縫、鞋店、配件店共 5 家）；改為**每個地區只保留一家衣物商店**，這家店對應**一個資源包**，包內可同時包含多種類別的衣物（上衣、下身、洋裝、外套、鞋、帽、配件，連**髮型**也放在同一個資源包、不再單獨設限）。店裡仍以類別分頁瀏覽各類商品。既有存檔已購買的衣物以衣物 id 記錄、不受影響（合併時保留 id）；被合併的店家、NPC、場景與題庫會一併清理，地圖佈點待 dev 以實機 visual-qa 校準。設計決策詳見 [docs/design-issue210.md](docs/design-issue210.md)。本項為 dev 實作，待 opr 終驗。
- 2026-06-20（issue #205）：設計把打工「Work」消失的時機從「答對」改為「**實際賺到 coins**」。承 #177（同一遊玩週期內每個場景的打工最多答對一次、答對後該場景的 Work 在這個週期消失），原本只要答對就消失——包含**用了中文協助或試了三次以上才答對、其實沒拿到 coins** 的情形，等於「沒賺到錢也算做完」，本週期就再也不能在那個場景賺錢。改為**只有本次真的拿到 coins（>0）才讓該場景的 Work 在這個週期消失**；若答對但沒拿到 coins（中文協助／第三次以上），Work **不消失**、本週期還能再作答去賺。每個場景每週期仍然「賺到一次就消失」，與 #177 的上限一致，只是把「答對卻沒賺到」從「算完成」改為「還能再試」。答案選項順序**沿用現有做法**（每次進入打工都會重新隨機排列），本案不另外調整。只影響打工（答對發 coins）；生活聊天不受影響。已以 `?selftest=job-cycle`（新增「0 coins 答對不下架」步驟）與 chat／playtimer／scene-nav／map-avatar／data-audit／save-load／monkey(300) 回歸自測，0 console error。設計決策詳見 [docs/design-issue205.md](docs/design-issue205.md)。本項為 dev 實作，待 opr 終驗。
- 2026-06-20（issue #196）：設計把每件衣物收斂為**單一一張 `512×512` 圖**，既當穿到公主身上的投影素材、也直接當商店方塊預覽，不再另外做一張 256×256 預覽縮圖。過去每件衣物有兩張圖（投影圖＋商店縮圖），兩張尺寸／裁切不同，商店看到的大小和實際穿上身的感受差很多。本案承 #195「單品單層」、並與 #197「資產標準」調和：衣物統一為 `512×512` 透明 WebP、**長邊貼滿**（長形如長褲等比縮放、至少一軸不留白邊、不變形），商店預覽與實際投影同源。素材改以**影像模型（OpenAI 生圖）重繪**取得清晰畫質，並用**三層描述詞**（全作品風格＋各素材包風格＋單品描述）驅動，使同一素材包風格一致、不同單品有差異；維護工具新增「描述詞」與「重生」按鈕、生成紀錄寫進圖檔本身。投影到身上的精確位置仍由維護者校準。屬美術翻新方向（約 80 件逐件重繪＋重新定位）。設計決策詳見 [docs/design-issue196.md](docs/design-issue196.md)。本項為 2plan 初稿，待 dev／opr 校準。
- 2026-06-20（issue #204）：改善打工與生活聊天題目品質。確立準則——**打工題公主的回應一定要是經過思考的決策、判斷或建議，不能只是把場景人物的指令或眼前動作覆述一遍**（例如把「我正在澆花，請告訴我我在做什麼？→你正在澆花」這種無意義複述題，改成「白花賣不完該怎麼辦？→多進白花／改賣別種花／不賣花」這種要拿主意的題）；題幹要留給公主判斷的空間，三個選項都是情境內合理但不同的決策、沒有跟情境無關的怪選項。生活聊天則維持自然的社交／情感回應（合理寒暄不算笨題），同樣不複述、不超現實。已落地：四地區（urban／rural／wild／castle）**42 道打工複述題改寫為思考決策題**（保留既有數學計算／比較等思考題；wild 保留關係子句／條件句文法但改為決策）、**57 個生活聊天超現實干擾選項改為情境內回應**（含中文）；`?selftest=data-audit` 新增「打工正解非題幹複述」與「聊天干擾須同場景語域」兩道守門防再現，data-audit／chat／job-cycle／monkey／save-load 等 selftest 全綠、0 console error。設計決策見 [docs/design.md](docs/design.md)（spec#1 補述、solCase#1.1／#14.1／#14.2、intTest#36／#38、spec#1 成效）。本項為 dev 實作，待 opr 終驗。
- 2026-06-20（issue #197）：為各類圖像資產建立**標準尺寸與檔重預算**，並擴充 GATE 資產 lint 全面把關，杜絕誤放過大圖檔拖慢遊戲載入。過去 `?selftest=data-audit` 只檢查部分資產的「像素尺寸」（公主 base、地圖、場景），沒有任何「檔案大小（檔重）」上限，也未涵蓋場景人物像、衣物縮圖／layer 與 UI；因此一張尺寸正確卻過重的圖（例如 800KB 以上的地區地圖）會直接放行，正是某些圖片載入緩慢的主因。本次把「資產類別 → {標準像素尺寸, 檔重上限}」收斂為單一標準表（`paramAssetStandards`，固定畫布類驗等於、衣物緊貼裁切類驗容於畫布），讓 lint 對**所有**圖像資產同時檢查尺寸與檔重；並把現存 9 張超標的過大圖檔（rural／urban／wild 地區地圖與 6 張 wild 場景）重壓縮至預算內（維持尺寸與童話手繪觀感，省約 2.3MB）。已以 `?selftest=data-audit`（250 資產、0 超標）、`save-load`、`monkey`(300 步) 與重壓縮資產實機畫質 before/after 驗證，0 console error。設計決策與 GATE 紀錄詳見 [docs/design-issue197.md](docs/design-issue197.md)。本項為 dev 實作，待 opr 終驗。
- 2026-06-20（issue #199）：已強化角色立繪輪廓辨識。角色透明背景 PNG/WebP 疊在複雜場景上時，常態可讀性改以貼合 alpha 外框的深色描邊與自然景深陰影處理；ADV NPC、公主紙娃娃、地圖 token 與頭胸照依 surface 分級校準，避免只靠一圈大範圍亮色光暈造成糊化。試穿光暈保留為互動狀態提示，與常態角色輪廓分離。已以 `?selftest=character-silhouette`、`map-avatar`、`data-audit`、`scene-nav`、`monkey` 與手機直向／桌機截圖驗證，0 console error。設計決策已納入 [docs/design.md](docs/design.md) 與 [contract-local/hmiIntf自訂角色尺度與美術規範.md](contract-local/hmiIntf自訂角色尺度與美術規範.md)。本項為 dev 實作，待 opr 終驗。
- 2026-06-20（issue #195）：已移除「整套穿搭（outfit set）」一鍵換整套的商品，外觀獎勵回歸逐件單品（髮型／衣服／鞋帽／配件）購買與穿戴；披風／外套也統一為單層呈現（移除未使用的背片圖層）。確立「單品單層」原則——每件衣物只對應一個外觀層，不再有整套綁定或單件多層，讓換裝的圖層表現與管理更單純一致；玩家既買的單品與既有進度都保留。並把原本螢光綠、被下架的「Rose royal cardigan」外套改色為柔玫瑰粉後復原（補足外套品項）。已以 `?selftest=data-audit`（各類 10、單品單層不變式）、`save-load`、`monkey` 與買裝外套實機驗證，0 console error。設計決策詳見 [docs/design-issue195.md](docs/design-issue195.md)。本項為 dev 實作，待 opr 終驗。
- 2026-06-19（issue #179）：已重繪並替換 16 張上下區域疑似模糊補版的 ADV 場景背景（維持單張 `1024x1024` WebP 與現有 sceneArt renderer），不新增上下模糊、延展、frosted cover、CSS 濾鏡或 renderer 特例；`?selftest=data-audit` 會列出 40 張 unique runtime sceneArt、檢查實際尺寸，並以 top／middle／bottom band detail heuristic 標記需要人工美術複核的低細節上下區域。新增 `?selftest=visual-qa&surface=scene-art-contact-sheet` 產出全場景 contact sheet，並可用 `?selftest=visual-qa&surface=scene-art&place=<id>` 截取單一場景手機直向／桌機畫面；本次重繪後 browser QA 為 40/40 checked、0 review warning、0 console error，QA 報告落於 [docs/test-summary.pdf](docs/test-summary.pdf)。設計決策詳見 [docs/design-issue179.md](docs/design-issue179.md)。
- 2026-06-19（issue #181）：把各地區打工報酬的難度間差距再縮小。承 #157（已把報酬由陡峭階梯壓為城堡 50／市區 60／鄉村 70／森林 80、最大約 1.6 倍），本次再收斂為**城堡 100／市區 105／鄉村 110／森林 115**（基數 100、每升一級 +5、最大僅約 1.15 倍）——讓地區間報酬「有差但不大」，避免小朋友只為了賺錢就硬刷最難地區亂猜。難度序沿用既有英文分級由易至難（城堡最易→森林最難）。商店定價維持 #157 不動（報酬墊高後商品在勞動題數上更易負擔，且報酬已近齊平、地區定價階梯不再誘導衝高報酬地區）。詳見 [docs/design-issue181.md](docs/design-issue181.md)。本項為 2plan 初稿，待 dev／opr 校準。
- 2026-06-19（issue #180）：設計讓公主在地圖上永遠顯示在最前面、不被地圖圖示遮住。過去公主在世界／城堡／地區地圖上移動時，會被地圖上的地點圖示（圓形／方形／門形標記）或裝飾性動畫元素（鳥、船、波浪等）擋住，看起來不對；原因是公主圖示的圖層高度（z-index 3）被排在這些地圖圖示之下。改為**把公主圖示固定排在所有地圖圖示之上**，讓公主走到任何地點圖示或裝飾元素上時都完整顯示、不被遮擋（但仍不會蓋過地圖上的操作面板，例如世界地圖的目的地清單）；地點仍可正常點擊進入場景，公主圖示放大與無背板（#161）等現況維持不變。範圍涵蓋地區、城堡與世界三處地圖。設計決策詳見 [docs/design-issue180.md](docs/design-issue180.md)。本項為 2plan 初稿，待 dev／opr 校準。
- 2026-06-19（issue #178）：已改善鍵盤在地圖上走路的手感。過去用方向鍵走路時，按下後要等一段「初始停頓」才連續走起來（因為連續走動是靠作業系統的按鍵自動重複、有內建延遲），而且每步移動量偏小、走得慢。改為由遊戲**自己管理「按住中的方向」並以連續移動迴圈推進**——按下方向鍵即時起步並持續移動、不再等系統重複延遲，並**加快走路移速**（每步位移量由 1.45／1.35／1.6 提高為 2.0／1.9／2.2，仍能停在地點上進入場景）；鬆鍵、切換分頁、視窗失焦或離開地圖即停，避免卡走。範圍涵蓋地區地圖、城堡地圖與世界地圖三處鍵盤走動（含方向鍵與 WASD），指標拖曳與「世界地圖點選走到再進入」維持不變。已以 `?selftest=map-walk`（即時起步、連續多步、放鍵即停、忽略系統自動重複、三面座標改變）與端對端真實按鍵探針自測，並回歸 `map-avatar`／`scene-nav`／`monkey` 等。確切移速數值仍可由 opr 實機 playtest 微調。設計決策詳見 [docs/design-issue178.md](docs/design-issue178.md)。
- 2026-06-19（issue #167）：已統一場景對話方塊的選項樣式並讓選購時看得見試穿。生活聊天、打工、逛店與退款等對話的選項／命令鈕配色**收斂為同一套**（沿用對話選項的暗色玻璃樣式，消除逛店／退款原本另一套粉紅漸層鈕造成的分歧）；逛店／退款／換裝這類背後有試穿的對話方塊**背景改得更透明（約現行一半）**、商品縮圖白底改為 `20%` 半透明白，讓公主換上衣服的試穿效果透出來看；退款若沒有可退的東西就**直接留空、不再出現突兀的白色方塊**（仍保留一行輕量提示）；對話裡公主與場景角色的左右位置改為**依螢幕比例**擺放（手機窄版 25%／75%、桌機寬版 33%／66%）。透明的是對話方塊背景，選項按鈕本身維持清楚可讀。已以 `?selftest=map-avatar`／`?selftest=data-audit` 與逛店／退款（含空清單）／生活聊天之手機直向＋桌機 visual QA 驗證。設計決策詳見 [docs/design-issue167.md](docs/design-issue167.md)。本項為 dev 實作，待 opr 終驗。
- 2026-06-19（issue #177）：已實作杜絕同一遊玩週期內重複刷錢。過去在同一個遊玩週期內（玩到時間用完、休息一次為一個週期），同一個場景的打工可以反覆作答、反覆答對、反覆賺 coins；現改為**每個場景的打工在同一個遊玩週期內最多只能答對賺一次**——某場景打工答對後，該場景選單的「Work」打工選項即在這個週期內消失（不能再作答；地圖目的地標示與提示文案也不再把該處標為可打工），等休息後進入下一個遊玩週期才重新出現、可再作答。只針對打工（答對發 coins）；生活聊天（答對加心情並延長遊玩時間、不發 coins）不套用此限制。已以 `?selftest=job-cycle` 自測（答對後下架、跨週期重置、舊存檔相容、聊天不計入）與場景選單實機驗證。設計決策詳見 [docs/design-issue177.md](docs/design-issue177.md)。
- 2026-06-19（issue #176）：修正衣物對位機制，讓任意尺寸的衣物素材都能正確穿到人物身上。過去衣物 layer 是把「整張圖（含周圍透明留白）」等比縮到接近整個畫面的範圍，所以當不同來源的同類素材畫布大小、留白或主體位置不一致時，衣服就會穿歪、變大變小。改為**每一類（髮型／上衣／下身／洋裝／外套／鞋／頭飾／臉部／頸／手共 12 類）可各自設定它在主畫面（512×768）上該投影到的目標範圍，素材一律等比縮放、落進該範圍**，無論原圖多大多小都對位到正確位置；同類共用同一範圍、新增同類衣物不必逐件微調。已強化 **Wardrobe Tuner 工具**（`tool/wardrobe-tuner.html`）讓維護者以肉眼逐類手動設定目標範圍、用「任意尺寸測試圖」即時預覽並匯出設定；引擎新增向後相容的目標範圍渲染（未設定的類別維持原本滿版行為，既有已調好的衣物不變）。各類別的確切目標範圍數值由維護者用工具逐類校準後套用。設計決策詳見 [docs/design-issue176.md](docs/design-issue176.md)。本項為初稿，待 dev／opr 校準。
- 2026-06-19（issue #164）：設計擴充場景內切換時的語音行為。承 #156（離開場景即收束語音），再補兩點：①在**場景內進出子互動（聊天／逛店／打工等）或返回場景選單**時，也即時停止前一段正在播放的語音、改接當下話題，不讓上一段聲音殘留跨層級；②**同一個場景的歡迎詞每次造訪只播一次**——首次進入場景時角色會打招呼，之後在子互動與場景選單之間往返不再重複播放，離開場景回地圖後再次進入才會重新招呼一次。沿用 #156 之即時停止降級（瀏覽器無法對進行中語句淡出時改以即時停止、不中斷遊戲）。設計決策詳見 [docs/design-issue164.md](docs/design-issue164.md)。本項為 2plan 初稿，待 dev／opr 校準。
- 2026-06-19（issue #168）：已實作 wardrobe 服裝對位與素材規則。服裝、鞋帽與配件 layer 依 `wardrobeLayerBoundsByType` 類別級 render bounds 與 `safeBox` 穿到 `shared-512x768-v1` 公主 base 上，同類衣物共用對位範圍，不以每件單獨 nudge 或 CSS 特例處理；`data-audit` 會檢查 layer type、bounds、alpha safeBox，以及正式 wardrobe layer／商品縮圖必須為 GPT 產生／修圖的 PNG／WebP 童話手繪 bitmap、禁止 SVG 作為正式服裝素材或完成品替代。已以 `?selftest=data-audit` 與 wardrobe-detail 手機直向／桌機 visual QA 驗證。
- 2026-06-19（issue #163）：已實作預設可玩公主與初始主題修訂。Yumi 改為深藍髮；原 Sol 對外改名 Mary 並改為深綠髮，但保留內部 `sol` id 以相容舊存檔；新帳號的識別色與背景花紋改為一次性隨機並保存。角色 base 以 GPT 產生／修圖來源校準為童話手繪風格 raster 素材，不使用 SVG、CSS 濾鏡或 renderer 特例代替；除指定髮色與使用 Rosa 眼睛校準 Mary／Yumi 外，未改動服裝、姿勢、比例、透明底與 rig 對位。已以 `?selftest=profile-color`、`?selftest=data-audit` 與選角 visual QA 驗證。
- 2026-06-19（issue #161）：設計調整地圖公主圖示。地圖上的公主圖示放大約一倍使其更醒目，並移除腳下的識別色橢圓背板讓畫面更簡潔；圖地分離改倚賴角色本身既有的深色投影與白描邊，地圖上不再以背板呈現各帳號識別色（識別色仍用於大頭照卡片底色與帳號辨識）。屬 spec#9 地圖 token 標示之精修。設計決策詳見 [docs/design-issue161.md](docs/design-issue161.md)。本項為 2plan 初稿，待 dev／opr 校準。
- 2026-06-19（issue #166）：設計讓地圖一眼看出哪些地點可以逛店買東西。地圖上的地點圖示原本全是圓形，看不出哪些有賣東西；改為**有販售（可逛店）的地點以方形圖示呈現**，與一般地點（圓形）、城門／傳送（門形）區隔，讓孩子一眼就找得到可以用 coins 換外觀的商店。規則依場景是否有商品自動套用、各地區（含城堡）一致，新增商店也會自動變方形。確切的方形樣式與大小待 dev 以實機 visual-qa（寬＋窄、四區含城堡）校準。設計決策詳見 [docs/design-issue166.md](docs/design-issue166.md)。本項為 2plan 初稿，待 dev／opr 校準。
- 2026-06-19（issue #165）：簡化生活聊天完成回饋。完成一次生活聊天時的回饋訊息由「Nice chat! +1 mood, +1 min play time.」收斂為僅顯示「+1 mood」，移除招呼語與遊玩時間提示；心情加值與護眼時長延長機制不變，延長量仍可在人物資訊欄 Play time 欄位（如 `15 +1😄 min`）看見。詳見 `docs/design-issue165.md`。本項為 2plan 初稿，待 dev／opr 校準。
- 2026-06-19（issue #169）：修正帳號選擇的休息鎖定。過去若一個帳號的遊玩時間在你離開（停在帳號選單）時用完，帳號卡會誤顯示「Play 0:00（看似可玩）」，一點擊卻立刻被鎖「還要等 15 分鐘休息」，且休息是從「重新選取的那一刻」起算、離開的時間完全不算。現修正為：**休息從「遊玩結束」起算，離開（沒在玩）的時間計入休息**——離開已達休息時長即視為休息足夠、可直接續玩；帳號卡也據實顯示「Rest 剩餘時間」或「Ready」，不再誤顯示「Play 0:00」，且帳號選單開啟時休息倒數會**隨時間實際遞減、屆滿即時轉為 Ready**（不再是開啟當下的凍結快照）。屬護眼休息（spec#9）邏輯修正、不改既有「玩滿就休息」的精神。
- 2026-06-18（issue #156）：設計修正離開場景時的語音收束。過去在場景觸發角色配音或公主朗讀後離開場景（關閉對話、切換場景或返回地圖），語音會繼續念到結束、殘留到下一個畫面；改為**離開場景時即時停止正在播放的語音、不殘留跨場景**。原訴求為「1 秒內漸變小聲停止」，惟瀏覽器 Web Speech API 無法對正在朗讀的語句逐漸調整音量（只能立即停止），故以**即時停止**作為該淡出聽感的明確降級（呼應既有「受瀏覽器語音能力限制時明確降級、不中斷遊戲」），交付重點為消除跨場景殘留。設計決策詳見 [docs/design-issue156.md](docs/design-issue156.md)。本項為 2plan 初稿，待 dev／opr 校準。
- 2026-06-18（issue #157）：打工報酬與商店定價再平衡。各地區打工報酬由陡峭階梯（城堡 20／市區 100／鄉村 500／森林 2000）改為平緩等差（城堡 50／市區 60／鄉村 70／森林 80），避免直衝高報酬地區洗 coins、讓四區循序漸進；商店定價同步壓平為相稱的平緩地區級距——城堡／市區維持現價，鄉村（舊 520–560→150–170）與森林（舊 2050–2400→190–290）的失真高價降回階梯內，市區一件異常套裝（wildTrailSet 2450→500）回到套裝帶，使各地區商品皆於數題勞動內可負擔、最大價差由約 25× 收斂至約 2×。詳見 `docs/design-issue157.md`。本項為 2plan 初稿，待 dev／opr 校準。
- 2026-06-18（issue #153）：設計修正選角流程與地圖公主可視性。①自帳號選擇「Add player」進入創角時，於**已有其他帳號**的情況下提供「取消／返回人物選擇」路徑（取消不殘留剛建立的空帳號）；**真正首次進入（尚無任何帳號）**仍須先建立一位公主、不可取消。②地圖上移動公主腳下的識別色橢圓背版改得更清楚可辨（提升可視性、保留各帳號識別色仍可區分），讓公主在地圖上一眼可見。設計決策詳見 `docs/design-issue153.md`，確切互動文案與背版數值待 dev 以實機 visual-qa（寬＋窄）校準。本項為 2plan 初稿，待 dev／opr 校準。
- 2026-06-18（issue #155）：設計強化對話自然性。承 #149 第一人稱對話，進一步把公主的回應改得更口語自然（可適度加語氣詞／生活用語、不再像教科書例句）；當場景角色拜託公主幫忙時，公主的正解一律先以自然的應允語句開頭（如「好啊，我可以…」「沒問題」「我來幫你」，對應 `Sure thing`／`OK, I can …`／`Well, I think I can help …`）再接實質回答，使幫忙回應親切一致。各區英文分級不變、語氣不超綱；改寫規則詳見 [docs/design-issue155.md](docs/design-issue155.md)。本項為 2plan 初稿，待 dev／opr 校準。
- 2026-06-18（issue #125）：設計重新配置地圖地點位置。各地區地圖（城堡／市區／鄉野／野地）的地點改為對應地圖背景藝術元素（出口在城門或道路、釣魚在河邊、商店在城內等）並相互不過度群聚，提升場景探索的空間一致性與沉浸感；確切座標待 dev 以實機 visual-qa 逐圖逐點校準。本項為 2plan 初稿，待 dev／opr 校準。
- 2026-06-18（issue #149）：場景對話全量改寫定調。歡迎詞、生活聊天與打工任務的對白一律改為場景角色以第一人稱對公主發話（取代第三人稱旁白），題幹為角色台詞、選項為公主的回應或任務回報（取代 Pick／Tell 等考試式指令）；打工任務每場景固定 3 題、選項由 4 收斂為 3，生活聊天每場景固定 2 題，數學包進角色任務情境；Castle 的 King Rowan／Queen Mira 定位為公主的父王／母后。各區英文分級不變（Castle Dolch、Urban Starters、Rural Movers、Wild Flyers）。改寫規則詳見 `docs/design-issue149.md`。本項為 2plan 初稿，待 dev／opr 校準。
- 2026-06-18（issue #143）：統一場景互動的離開動線。場景互動明確分兩層——第一層場景選單用 `Leave` 回地圖，進入任一互動（生活聊天／打工／逛店／退款／換裝／提示）後一律用 `Back` 回到場景選單；解決過去「聊天完想買東西卻只能直接跳出場景」的問題，讓同一次進場可在各互動間往返、只有在場景選單按 `Leave` 才退出場景。本項為 2plan 初稿，待 dev／opr 校準。
- 2026-06-18（issue #134）：改善角色語音。新增可於設定（Character voices）為各角色類型（依性別×性格）指定瀏覽器可用語音的「音色挑選層」（採同性別繼承式預設、可逐桶覆蓋），解決過去因 voice hint 比對失效而「不同人物聲音幾乎一樣」的問題；未指定時沿用語言優先 fallback（不硬綁具名 voice、維持跨平台可攜），年齡等差異續以音高／語速表現；並在送入 Web Speech API 前於文字開頭加入少量前置留白以改善首字清楚度。已於 dev 實作（`?selftest=voice` 驗證指定／繼承／缺 voice 降級／首字留白／設定 UI），待 opr 終驗。
- 2026-06-17（issue #131）：設計強化公主主題顏色應用。識別色色盤由 16 高飽和色改為 8 種低飽和粉彩色，並新增調色器供自訂任一色（既有存檔識別色相容保留、不被重置）；新增可選背景花紋（波浪／泡泡／格紋等 ≥8 種）與識別色組成公主視覺主題；選角卡、資訊欄與帳號卡的大頭照底色改用識別色半透明鋪底（約 0.45，經實機 QA 校準）；地圖公主 token 由糊化光暈改為半透明識別色橢圓背版。本項為 2plan 初稿，待 dev／opr 校準。
- 2026-06-17（issue #138）：設計對話內容調整。生活聊天改為每個可互動場景（含商店）預設都能進行（公主房／城門除外），消除商店特例；生活聊天每題收斂為 2 選項、打工任務維持 4 選項；打工題庫全面稽核，題目／開場白與干擾選項須切合場景主體、移除超現實與「需要一個英文字」這類 meta 旁白。本項為 2plan 初稿，待 dev／opr 校準。
- 2026-06-17（issue #135）：設計場景對話改版。每個場景改用單一模板、可選擇性開啟生活聊天／逛店／打工三種互動；題庫改為貼合場景主體、依地區英文等級做句型分級並生活化（取代跨場景可互換的樣板與超現實選項）；生活聊天答對提升心情並在護眼上限內延長可玩時間（社交為滿足自我需求），打工任務以切合場景、可含簡易數學／生活常識的題目賺取 coins。本項為 2plan 初稿，待 dev／opr 校準。
- 2026-06-16（issue #109）：已實作 Web Speech API 語音品質改善。中英文語音目標改為約 80% 語速；新增 voice 載入與 `voiceschanged` fallback、語音佇列／replace-last、避免無條件 `cancel()` 截斷、autoplay/user activation 與錯誤碼降級處理，以及語音診斷紀錄；已以 `?selftest=voice` 驗證，待 opr 依實機聽測校準首字清楚度與角色可辨識度。
- 2026-06-16（issue #126）：已實作兒童休息與帳號／公主辨識流程修訂。遊玩與休息預設改為各 15 分鐘；人物資訊欄顯示本次開始時間與剩餘可玩時間；休息／結算與遊戲內提供返回初始帳號／公主選單的入口但不得繞過休息鎖定；公主選單、帳號卡與人物資訊欄共用頭胸部大頭照與 profileColor，四位公主有不同預設色且可由玩家從 16 色色票調整，地圖公主光暈同步使用該識別色。已以 `?selftest=profile-color`、`?selftest=playtimer`、`?selftest=accounts`、`?selftest=map-avatar` 驗證。
- 2026-06-16（issue #123）：依使用者提供的四張 PNG 重製四位可玩公主 base，轉為透明 WebP 並縮放對位到 `shared-512x768-v1`；選角畫面直接裁切 `base.webp` 頭胸部特寫，不再使用角色 `thumb.webp`；新 base 採 baked-in 短髮、粉紅短袖上衣與紅短褲，預設 starter 髮型／服裝不再額外疊圖，保留 `lumi`／`yumi`／`sol`／`rosa` id 相容。待 opr 終驗。
- 2026-06-15（issue #110）：設定選單新增 About 頁籤，集中呈現作品版權宣告（`carlton0521@gmail.com, copyright reserved, 2026`）與歷次版本的中文短主旨（預設最近 10 版）；既有版本與建置時間併入 About，Settings 不再另列版本卡。本項為 2plan 初稿，待 dev／opr 校準。
- 2026-06-15（issue #99）：統一各地圖的公主頭像顯示與控制。World Map／Castle Map／各地區地圖都有可見、可移動的公主（鍵盤方向鍵走動）並加強頭像辨識度；在 World Map 點選地點後公主會先走過去再進入，移動途中再點一次可略過直接進入。本項為 2plan 初稿，待 dev／opr 校準。
- 2026-06-15（issue #100）：答題獎勵統一收斂為只發 coins，移除原本附帶的字彙／表達／善良／心情屬性獎勵與答對後的「選擇獎勵」導購捷徑，所有場景答對一致直接給 coins，簡化獎勵邏輯。本項為 2plan 初稿，待 dev／opr 校準。
- 2026-06-14（issue #102）：語音整體放慢為原本速度的約 3/4（英文與中文協助一併放慢），讓兒童更容易聽辨，各角色相對語速差異維持。本項為 2plan 初稿，待 dev／opr 校準。
- 2026-06-14（issue #93）：設計新增角色差異化配音。不同場景人物依特性（性別／年齡／性格等）以各自聲音念對白與開場，玩家公主在作答時以其聲音唸出所選選項；以瀏覽器內建語音（音高／語速為主）呈現、缺設定自動降級，沿用既有 Voice 開關。本項為 2plan 初稿，待 dev／opr 校準。
- 2026-06-14（issue #106）：移除既有 Help 提示功能與 OpenAI 設定入口；英文練習改由地點內的 Practice 動作直接開始，保留題目／選項英文與中文語音協助。
- 2026-06-14（issue #73）：新增中文協助與獎勵階梯。答題時題目與每個選項都可撥放英文或中文；未用中文且越早答對獎勵越高（第一次全額、第二次半額），用過中文或第三次以後答對則該題無獎勵，鼓勵以英文為主、中文為輔。已於 dev 實作（castle/urban/rural/wild 四地區全雙語、缺中文自動降級、`?selftest=chinese-reward` 驗證）；待 opr 終驗。
- 2026-06-13（issue #88）：導入 2tech 設計方法論。新增內部設計 SSOT [docs/design.md](docs/design.md) 與 [scripts/docLint.ps1](scripts/docLint.ps1)；README 改寫為產品手冊；角色尺度與美術規則拆入 contract-local 契約。先前的詳細開發／架構／QA 內容見本次變更前的 README（git 歷史）與待補契約清單。
- 2026-06-13（issue #63）：設計新增本機多帳號（Account Select）。進入遊戲先選帳號，可新增／刪除帳號，各帳號 coins、日記、穿搭與進度互不混用；明確不做網路登入／密碼／雲端同步。本項為 2plan 初稿，待 dev／opr 校準。
- 2026-06-13（issue #6）：設計新增遊玩時間限制與護眼休息。連續遊玩達設定時長（預設 10 分鐘、可調）會結算本回合成果並進入強制休息（預設 10 分鐘），休息結束前不可續玩；遊玩時間即各帳號的 energy 預算、各帳號各自計算。本項為 2plan 初稿，待 dev／opr 校準。
