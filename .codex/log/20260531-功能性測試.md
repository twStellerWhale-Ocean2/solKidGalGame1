# 20260531 功能性測試

## Scope

- Room to Map
- Map movement and hotspot entry
- ADV wrong/correct answer behavior
- Shop preview / buy / leave
- Wardrobe return
- Diary records

## Initial Findings

- Local server for this repo is `http://127.0.0.1:4176/` because `4174` was already occupied by another node process.
- Save/load selftest passed:
  - `{"test":"save-load","passed":true,"markdownLength":1213,"beforeDifficulty":100,"afterDifficulty":100,"beforeCoins":100,"afterCoins":100}`
- Monkey selftest initially wedged browser control because random button clicking could hit native confirm buttons (`Reset Progress`, `Clear Diary`).
  - Fix: excluded `clearDiaryButton` and `resetButton` from monkey random visible-button clicks.
  - Retest through headless Chrome fallback passed:
    `{"test":"monkey","passed":true,"steps":300,"errors":[],"coins":10,"playerNode":"farm","activeQuest":"accessoryShop","activeViews":1}`
- `node --check script.js` passed after each script edit.
- Current complete-test pass uses `http://127.0.0.1:4177/` because `4176` served stale cached HTML/assets for the active working tree.
- Browser/iab opened and verified the current worktree URL.
- Save/load selftest on `4177` passed:
  - `{"test":"save-load","passed":true,"markdownLength":1196,"beforeDifficulty":100,"afterDifficulty":100,"beforeCoins":100,"afterCoins":100}`
- Monkey selftest on `4177` passed:
  - `{"test":"monkey","passed":true,"steps":300,"errors":[],"coins":10,"playerNode":"castleRoom","activeQuest":"farm","activeViews":1}`
- Final rerun after cache-busting query bump:
  - Save/load: `{"test":"save-load","passed":true,"markdownLength":1474,"beforeDifficulty":100,"afterDifficulty":100,"beforeCoins":10,"afterCoins":10}`
  - Monkey: `{"test":"monkey","passed":true,"steps":300,"errors":[],"coins":10,"playerNode":"farm","activeQuest":"farm","activeViews":1}`
- Browser console health after selftests: no `error` or `warn` entries returned.

## Revisions

- Fixed Wardrobe / Shop item card previews by changing inline `--item-img` from truncated `url("...")` HTML to `url(...)`.
- Fixed monkey test stability by preventing random clicks on native confirm actions.
- Enlarged mobile map coordinate metrics and visual map layer together so hotspots remain aligned after the mobile map scale change.
- Restored `assets/kingdom-map.png` to the previous hand-drawn Git version after the user identified the geometric map as unacceptable.
- Added cache-busting query to the map image reference in `index.html`.
- Bumped the `script.js` query in `index.html` so the current script is not masked by stale browser cache.

## Residual Risk

- Manual long-form Room -> Map -> every quest -> every shop route was not completed in this pass because the in-app Browser controller became unstable after the original native confirm monkey run. Automated save/load and monkey results are passing.
- Current manual pass improved evidence but still did not fully close natural keyboard hotspot activation: after moving toward Sunny Farm, the objective showed the farm quest, but pressing Enter / Space did not open the ADV scene in that attempt. This remains a focused manual-flow issue to fix or retest separately.
