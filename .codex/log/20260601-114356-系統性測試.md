# 20260601-114356 系統性測試

## 靜態與伺服器檢查

- `Invoke-WebRequest http://127.0.0.1:4174/`：HTTP 200。
- `node --check .\src\main.js`：通過。
- `Get-ChildItem -Recurse .\src -Filter *.js | node --check`：通過，無語法錯誤輸出。
- 初始有兩次 PowerShell quoting 失敗：`$_` 與 `|` 被外層命令誤解；依 PowerShell quoting skill 改用單引號內層 `-Command` 後重跑通過。該問題是命令包裝失敗，不是 app runtime 失敗。

## Browser gate

- 已讀並遵循 Browser plugin workflow。
- `agent.browsers.list()` 回報：
  - Chrome extension
  - Codex In-app Browser `iab`
- 已取得 `agent.browsers.get("iab")`。
- `iab` DOM 驗證、自測、互動與截圖皆成功。
- 本輪不需要外部 Chrome / Playwright fallback。

證據：

- `.codex/log/20260601-114356-qa/iab-browser-gate.json`
- `.codex/log/20260601-114356-qa/iab-dom-results.json`

## 自測結果

Save / Load selftest：

```json
{"test":"save-load","passed":true,"markdownLength":1642,"beforeDifficulty":100,"afterDifficulty":100,"beforeCoins":0,"afterCoins":0}
```

Monkey selftest：

```json
{"test":"monkey","passed":true,"steps":300,"errors":[],"coins":0,"playerNode":"princessRoom","activeQuest":"harbor","activeViews":1}
```

## Console health

- `iab-dom-results.json`：15 個 DOM row，warning/error logs 為 0。
- `iab-mobile-shot-results.json`：16 張手機截圖，warning/error logs 為 0。
- `iab-interaction-results.json`：互動流程 warning/error logs 為 0。
- `desktop-smoke.json`：桌機 smoke warning/error logs 為 0。

## Storage / Save 契約

- source check 保持 key：
  - `luminara-princess-english-adv`
  - `luminara-openai-help-settings`
- Save MD marker 保持：
  - `LUMINARA_SAVE_JSON`
- selftest 確認 Save MD 不含 `OPENAI_API_KEY`。
- Browser read-only evaluate scope 無法直接列舉 `localStorage.length`，已改以 source check + Save/Load selftest 作為契約證據。

## 系統性結論

系統性測試通過。#22 模組拆檔後沒有語法錯誤、runtime warning/error 或 selftest regression。
