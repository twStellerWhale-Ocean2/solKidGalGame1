# 20260531 Restart 功能性測試

# 緣起主旨

依使用者要求「重啟 skill 從最前面全部重做」，本檔記錄 restart pass 的功能性測試與修訂。

# 參考準備

- Source of truth: `AGENTS.md`, `README.md`, `m-skill-2tech-children-adv-game-dev`.
- Browser: Browser/iab.
- Current test URL: `http://127.0.0.1:4177/`.
- Screenshot folder: `.codex/log/20260531-restart-qa/`.

# 內容程序

## 主循環

### 測試項

- Room -> Map: passed via `Go to Map`.
- Map -> hotspot focus: initially failed to expose reliable interaction card / Enter flow.
- Quest ADV wrong answer: passed, wrong choice produced `Try again.`
- Quest ADV correct answer: passed, correct choice awarded `+100 coins`.
- Shop ADV Need-more-coins: initially failed because command label said `Buy 180 coins` while preview said `Need 70 more coins`.
- Shop ADV buy: passed after selecting `Blue harbor dress` and clicking `Buy 100 coins`; coins dropped from `110` to `10`, item became `Equipped`, diary recorded purchase.
- Diary after quest/shop: passed; quest and shop records visible.
- Save/Load selftest: passed.

## 問題#1

**問題說明**：Map 進入 hotspot 的操作不穩。初始測試中 objective 顯示任務目標，但 `nearbyCard` 未顯示，且按 Enter 沒有開 ADV。分類：Must Fix，影響功能性 / 介面性 / 好玩性。

**解決規劃**：在 `activeHotspot` 存在時顯示 `nearbyCard.show`，並在 window keydown 中補上 Map active + Enter/Space 的互動路徑，降低 focus 依賴。

**前後比較**：

Before:

![Before Enter attempt](20260531-restart-qa/desktop-map-hotspot-after-enter-attempt.png)

After:

![After Enter opens ADV](20260531-restart-qa/desktop-map-hotspot-after-fix-enter.png)

**修訂結論**：修訂完成。重測可從 Map hotspot 按 Enter 開啟 Sunny Farm Quest ADV。

## 問題#2

**問題說明**：Shop 不足金額狀態矛盾。Market preview 顯示 `Need 70 more coins`，但 command 顯示 `Buy 180 coins`。分類：Must Fix，影響 Shop reward clarity。

**解決規劃**：讓未擁有且金額不足的 shop command 顯示 `Need X more coins`，與商品 preview 一致；點擊後保留不足提示。

**前後比較**：

Before:

![Shop before label fix](20260531-restart-qa/desktop-shop-open.png)

After:

![Shop after label fix](20260531-restart-qa/desktop-shop-after-label-fix.png)

**修訂結論**：修訂完成。Market command 目前顯示 `Need 70 more coins`。

# 備註紀錄

全部 2 個功能 Must Fix：

* 完成改善 2 個
* 無改善方案 0 個
* 修訂失敗 0 個
* 未修訂 / 後續工程項 0 個
