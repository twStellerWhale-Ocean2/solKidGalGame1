# 20260531-170817 功能性測試

## 待驗項目

- Castle / Kingdom bottom area navigation 可切換。
- Castle map 可點 Princess Room marker。
- Princess Room scene 只顯示功能 action choices。
- Dresses / Accessories / Shoes / Room Treasures 可進入 wardrobe detail panel。
- Kingdom shop marker 先進 scene action choices，再由 Shop 進入商品 detail panel。
- 商品可 preview / buy / equip，購買後 feedback 與 Diary 更新。
- Diary / Settings / Save / Load 仍透過齒輪 overlay 開啟。

## 結果

- 通過。
- 使用 Browser plugin / in-app Browser 在 `http://127.0.0.1:4177/` 實際操作。
- 操作證據：`.codex/log/20260531-170817-qa/functional-flow-result.json`

## 實測流程摘要

- `Castle map` 開啟後 HUD 只有 `Coins / Energy / Level`。
- 點 `Enter` 進入 `Princess Room scene`，只顯示 `Dresses / Accessories / Shoes / Room Treasures / Go Outside`。
- 點 `Dresses` 後才進入 wardrobe detail panel；`Back` 可返回 room action choices。
- `Go Outside` 回 Castle map，`Kingdom` 切到 kingdom area map。
- `Dress Boutique scene` 只顯示 `Shop / Chat / Leave`。
- 點 `Shop` 才開啟商品 detail panel。
- 選 `Blue harbor dress` 後 `Buy 100 coins` 成功，HUD coins 由 `300` 變 `200`，feedback 顯示 `Blue harbor dress is on Lumi now.`。
