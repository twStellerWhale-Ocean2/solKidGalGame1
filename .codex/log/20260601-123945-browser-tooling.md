# Browser tooling record

## Browser plugin 優先 gate

- 已讀 `browser:browser` skill。
- 已透過 Node REPL 載入 Browser plugin 的 `scripts/browser-client.mjs`。
- `agent.browsers.list()` 結果包含 `Codex In-app Browser` (`iab`) 與 Chrome extension。
- 已成功取得 `agent.browsers.get("iab")`。
- 已成功用 `iab` tab 開啟 `http://127.0.0.1:4174/#home`，頁面 title 為 `solKidGalGame`。

## 失敗點

- `iab` 可導航與讀 DOM，但 `tab.screenshot({ fullPage: false })` 連續逾時。
- 具體錯誤：`Timed out running CDP command "Page.captureScreenshot" for tab 2`。
- 逾時時 DOM 驗證資訊：`url=http://127.0.0.1:4174/?qa=20260601-123945#home`、`title=solKidGalGame`、`body children=4`。

## fallback 記錄

因本輪 skill 要求手機直向截圖與美術性測試，且 `iab` 截圖能力無法完成 screenshot manifest，後續截圖與全量 visual QA 允許改用外部 Playwright / Chromium fallback。互動與 DOM 最小驗證已先由 `iab` 完成。
