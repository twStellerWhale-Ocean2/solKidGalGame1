# 20260601-114356 介面性測試

## 控制與操作

| 項目 | 結果 | 證據 |
|---|---|---|
| 手機觸控式大按鈕 | Pass | Room、Shop、Quest、System tabs 均可用 |
| Room action choice -> detail panel | Pass | `princess-room-click-dresses` |
| Shop action choice -> detail panel -> return | Pass | `boutique-click-shop-leave` |
| 鍵盤數字答題 | Pass | `quest-keyboard-number-2` |
| System tabs role | Pass | `Settings` / `Save / Load` 為 role `tab` |
| Back / return path | Pass | `system-tabs-settings-save-back` |
| active view invariant | Pass | 測試中 `activeViewCount` 維持 1 |
| console health | Pass | warning/error logs 為 0 |

## 手機直向可讀性

- 主要 gameplay 截圖 body width 為 391，未出現產品畫面水平溢出。
- `save-load-selftest` 與 `monkey-selftest` 的 raw JSON `<pre>` 讓 body width 到 943；這是 dev-only selftest 證據頁，不列為產品 UI 問題。
- Settings / Save overlay 文字可讀，#5 版本資訊清楚。

## 介面風格風險

- Settings 仍偏「設定表單」而非遊戲內道具頁，但它已收在 Lumi Diary overlay，不會佔用預設主畫面。分類：Should Fix。
- Shop detail 與 feedback panel 在 390px 寬度下可操作，但商品列表、文字與底部命令列視覺密度偏高。分類：Should Fix。
- Room / Shop / Quest 的 modal 建構功能清楚，但背景仍是 dimmed map，日式 ADV 場景感不足。此點放到美術性測試列 Must Fix。

## 介面性結論

操作性通過；沉浸式介面仍有 Should / Must Fix，不能宣稱完整 ADV 介面美術通過。
