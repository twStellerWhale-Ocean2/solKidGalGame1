# I. 緣起目的

驗證 Save / Load、Settings、狀態不變量與 console health，避免本輪美術與 CSS 修改造成系統狀態破壞。

# II. 參考準備

- `selftest-save-load-result.json`
- `selftest-monkey-result.json`
- `browser-selftest-logs.json`
- `chrome-cdp-capture-results.json`

# III. 內容程序

## Save / Load

結果檔：`.codex/log/20260601-135745-qa/selftest-save-load-result.json`

```json
{
  "test": "save-load",
  "passed": true
}
```

確認項：

- Save markdown 含 `## Diary` 與 `LUMINARA_SAVE_JSON`。
- Save markdown 未包含 `OPENAI_API_KEY`。
- Load 後 difficulty、coins、activeQuest、player position 可還原。

## Settings / Overlay

截圖：`mobile-settings-after.png`

結論：Settings 仍收在書本 overlay，不回到固定網站頁籤；OpenAI Help Teacher 欄位保留但不寫入 Save。

## 狀態不變量

Monkey selftest 結果檔：`.codex/log/20260601-135745-qa/selftest-monkey-result.json`

```json
{
  "test": "monkey",
  "passed": true,
  "steps": 300,
  "errors": []
}
```

檢查項：

- coins 未變負。
- active view 數量維持 1。
- 裝備不指向未擁有物。
- 隨機切換後仍可回到可操作畫面。

## Console Health

Browser plugin `iab` 初始截圖批次與 selftest log 均為 0 warning / 0 error。後續 fresh 重截時 `iab` screenshot API 逾時，已改用 Chrome CDP fallback；最終 `chrome-cdp-capture-results.json` 記錄 19 個 390x844 surface，且每個 surface 的 `consoleIssues` 皆為空，總 `consoleIssueCount` 為 0。

# IV. 備註紀錄

系統性測試結論：通過。未發現與本輪修改相關的 runtime error、Save/Load 破壞或狀態不變量破壞。
