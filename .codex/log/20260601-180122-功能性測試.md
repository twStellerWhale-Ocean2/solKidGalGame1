# 20260601-180122 功能性測試

## 目標

驗證本輪 Mobile Map Viewport Architecture v3 沒有破壞 Room、Map、ADV、Shop、Wardrobe、Diary 的核心流程，並特別檢查 Castle / Kingdom 地圖拖曳與縮放後的圖層同步。

## 初始狀態

- 本機服務：`http://127.0.0.1:4174/` 已有 listener，HTTP status 200。
- selftest URL smoke：
  - `/?selftest=save-load#home` HTTP status 200。
  - `/?selftest=monkey#home` HTTP status 200。
  - `/?selftest=visual-qa&surface=kingdom-map#map` HTTP status 200。
  - `/?selftest=visual-qa&surface=castle-map#home` HTTP status 200。
- Browser plugin 狀態：已讀 Browser workflow，但 `tool_search` 找不到 Node REPL JS 工具，無法取得 `iab`。

## 結果

- `node --check src/main.js` 通過。
- 所有 `src/**/*.js` 與 `server.mjs` 逐檔 `node --check` 通過。
- 靜態 HTTP 入口與 selftest URL 可被 server 回應。
- 未完成：Room 出門、Map 拖曳 / pinch、ADV、Shop、Wardrobe、Diary 的實際瀏覽器操作，原因是 Browser plugin gate 缺少 Node REPL JS 工具，且本輪未被允許改用外部瀏覽器工具。
- 結論：工程 smoke 通過；實際遊玩功能性測試未完成，不得宣稱 full pass。
