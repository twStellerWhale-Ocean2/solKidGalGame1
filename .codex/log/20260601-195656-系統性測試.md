# 20260601-195656 系統性測試

## 已執行

- JS syntax checks 通過。
- 本機 server 可回應首頁。
- Save / Load 自動入口已保留，未在 browser 中完成執行。

## 狀態不變量設計檢查

- coins 不足時 `buyItemInAdv` 不扣款、不加入 owned、不 persist。
- owned item branch 不再透過 Shop 切換 outfit，避免 Shop 與 Wardrobe 職責混用。
- 購買成功時才寫入 `state.outfit`。
- Shop preview 只存在於 `shopPreviewItemId` 與 `renderShopPreview`，離開 Shop 不會把未購買 preview 寫入 outfit state。

## 未完成

缺少 Browser 實際執行，因此尚未完成 Save / Load roundtrip、console health、active modal 可離開等瀏覽器系統性驗證。
