# 20260601-180122 美術性測試

## 目標

用手機直向玩家視角檢查 Castle / Kingdom 地圖底圖、marker、小公主 token、map actors 是否共用同一個 viewport，並確認本輪未以工程測試替代美術判斷。

## 檢查限制

- 本輪不新增或替換正式美術素材。
- 本輪聚焦地圖圖層對齊，不宣稱全遊戲美術掃描完成。
- 若 Browser plugin / 截圖工具不可用，必須明確記錄阻塞，該 manifest row 不得標 `Accept`。

## 結果

- 本輪已完成靜態美術風險降低：
  - 底圖與上層 marker / player / actor 共用同一 display / offset contract。
  - 移除底圖獨立 `--map-pan-x/y` transform，避免「底圖不動、圖示移動」的分層漂移來源。
- 未取得手機直向截圖。
- 未完成 manifest 中 10 個必測 row 的全尺寸截圖、固定檢查清單、10 點分組批評與前後比較。
- 阻塞原因：
  - 已讀 Browser plugin workflow。
  - `tool_search` 查找 Node REPL JS 工具結果為 `Found 0 tools`。
  - 無法執行 Browser bootstrap、`agent.browsers.list()`、`agent.browsers.get("iab")`、導航或截圖。
  - 依 AGENTS 瀏覽器政策，未使用外部瀏覽器工具 fallback。
- 第二次嘗試：
  - 使用者提醒需先開啟右側 in-app browser 視窗。
  - 本工具面沒有可用工具可直接打開右側 Browser 面板。
  - 重新查找 Browser / Node REPL JS tool 仍未取得可呼叫工具。
- 結論：美術性測試未完成，所有需要截圖證據的 row 仍為 `未完成`，不得宣稱 Accept 或美術 pass。
