# 20260601-195656 功能性測試

## 本輪模式

修正目前功能 + 測試內容：Shop 採購流程改為只列未購買商品，購買後從 Shop 清單消失，已購買商品回 Wardrobe / Room Treasures 管理。

## 已執行

- `node --check src/main.js`：通過。
- `node --check src/testing/selftests.js`：通過。
- `node --check src/flow/adv-controls.js`：通過。
- 本機 HTTP smoke：`http://127.0.0.1:4174/` 回 `HTTP 200 OK`。
- 新增 QA URL HTTP smoke 均回 `200`：
  - `?selftest=visual-qa&surface=shop-detail&place=boutique&item=blueDress#map`
  - `?selftest=visual-qa&surface=shop-not-enough&place=boutique&item=blueDress#map`
  - `?selftest=visual-qa&surface=shop-sold-out&place=boutique#map`

## 靜態功能檢查

- Shop 開啟時改用未購買商品作為 preview 起點。
- Shop grid 改為每列商品都有 preview item card 與 `BUY / Need N` 行動按鈕。
- 購買成功後商品加入 `state.owned`，wearable item 自動 equip，`shopPreviewItemId` 清空並重算清單。
- 已擁有商品不再在 Shop 中執行 equip branch。
- 全部購買後顯示 sold-out empty state 與 `Leave`。

## 未完成

Browser plugin 的 Node REPL 執行工具未暴露，無法完成實際點擊流程驗證；不得宣稱功能性測試全量完成。
