# 20260601-195656 介面性測試

## 已檢查

- Shop row 現在由商品 preview 與同列 BUY button 組成，符合「每個商品後面有 BUY」的操作模型。
- ADV focus selector 已加入 `.shop-buy-button`，鍵盤焦點可進入 BUY action。
- 手機樣式補入 `.shop-buy-row` 與 `.shop-buy-button` 尺寸限制，避免 BUY 被壓成不可點。

## 未完成

尚未能透過 Browser plugin 進行手機直向實際操作，因此焦點順序、觸控大小、文字裁切與返回流程仍需瀏覽器確認。
