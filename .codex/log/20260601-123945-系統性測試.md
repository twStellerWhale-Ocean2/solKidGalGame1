# 20260601-123945 系統性測試

## 結果

- 狀態：通過。
- 本地 server：既有 `server.mjs` 在 `http://127.0.0.1:4174/` 提供服務。
- Browser plugin：已先嘗試 in-app Browser；`agent.browsers.get("iab")` 成功，但 `Page.captureScreenshot` 逾時，已記錄於 `.codex/log/20260601-123945-browser-tooling.md`，因此改用暫存目錄中的 `playwright-core` 與系統 Chrome 對同一 localhost 驗證。

## 檢查項目

- `node --check` 全部 `src/**/*.js`：通過。
- `node --check server.mjs`：通過。
- `git diff --check`：通過，僅出現 Git 對 LF/CRLF 的換行提示，無 whitespace error。
- CSS asset URL 檢查：`styles` 內已無 `url("assets/...")` 造成 `/styles/assets/...` 404 的引用。
- 視覺批次的 `browser-console.json`：空陣列。
- 視覺批次的 `network-4xx.json`：空陣列。

## 修正項目

- 補正 CSS 內資產路徑為 `../assets/...`。
- 修正 `visual-qa` 未帶 `coins` 時誤把金幣設成 `0` 的測試入口問題。
- 補正商店購買後與 focus timer 造成的狀態/預覽不同步。
- 補正手機商店 layout 單欄時未重設 `grid-column`，導致 feature preview 被壓縮的問題。
