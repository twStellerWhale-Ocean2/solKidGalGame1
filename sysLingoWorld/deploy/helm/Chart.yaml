# 由 incrFlow genVersion（SSOT 工具）自根目錄 VERSION 投影生成——禁止手改（防漂移：--check）。
apiVersion: v2
name: sollingoworld-chart
description: solLingoWorld 整包自架部署（遊戲殼＋帳號存檔 API＋/admin/ 線上管理＋PostgreSQL；spec#27）
type: application
version: 0.77.0
appVersion: "0.77.0"
