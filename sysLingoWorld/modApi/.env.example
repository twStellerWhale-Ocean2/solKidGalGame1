# sysApi 環境變數（複製為 .env 或直接以環境變數供給；.env 不入版控）
# 對應 deploy/compose.yaml 之 PostgreSQL：
DATABASE_URL=postgres://luminara:luminara@127.0.0.1:5433/luminara
# session token 雜湊 pepper（自訂一段隨機長字串）：
SESSION_SECRET=change-me-to-a-long-random-string
# 選配：
# PORT=4180
# SESSION_TTL_DAYS=30
# RATE_LIMIT_MAX=10
# RATE_LIMIT_WINDOW_MS=600000
# STATIC_ROOT=../modShell   （遊戲殼靜態根；預設自動偵測——dev 源樹＝../modShell、發行 image＝/app，通常毋須設定）
# ADMIN_ROOT=../modAdmin    （/admin/ 管理頁靜態根；預設自動偵測同上，通常毋須設定）
